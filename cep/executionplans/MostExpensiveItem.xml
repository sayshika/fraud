<?xml version="1.0" encoding="UTF-8"?>
<executionPlan name="MostExpensiveItem" statistics="disable"
  trace="enable" xmlns="http://wso2.org/carbon/eventprocessor">
  <description/>
  <siddhiConfiguration>
    <property name="siddhi.persistence.snapshot.time.interval.minutes">0</property>
    <property name="siddhi.enable.distributed.processing">false</property>
  </siddhiConfiguration>
  <importedStreams>
    <stream as="input" name="transactionStream" version="1.0.0"/>
  </importedStreams>
  <queryExpressions><![CDATA[define table expensiveItem (itemNo string) from ('datasource.name'='FraudToolbox', 'table.name'='expensiveItem');

from input[not ((itemNo==expensiveItem.itemNo) in expensiveItem)]
select txnid, cardnum, txnamt, currency, email, shippingaddress, billingaddress, ip, itemNo, qty, timestamp, 0.0 as score, 1 as fraudflag
insert into outputStream;

from input[((itemNo==expensiveItem.itemNo) in expensiveItem)]
select txnid, cardnum, txnamt, currency, email, shippingaddress, billingaddress, ip, itemNo, qty, timestamp, 0.0 as score, 0 as fraudflag
insert into fraudStream;]]></queryExpressions>
  <exportedStreams>
    <stream name="outputStream" valueOf="outputStream" version="1.0.1"/>
    <stream name="fraudStream" valueOf="fraudStream" version="1.0.1"/>
  </exportedStreams>
</executionPlan>
